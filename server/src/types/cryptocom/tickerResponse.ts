import { CryptoComTickerData } from './tickerData'

export type CryptoComTickerResponse = {
  result: {
    data: CryptoComTickerData[]
  }
  // id: 1
  // method: 'public/get-instruments'
  // code: 0
}
